apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: todo
data:
  # TODO: injecter les vraies valeurs au runtime (kubectl create configmap ...).
  NODE_ENV: CHANGE_ME
  PORT: "3000"
  DB_HOST: CHANGE_ME
  DB_PORT: "5432"
  DB_NAME: CHANGE_ME
